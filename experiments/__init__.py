"""
Paper benchmark and figure pipeline (``torch`` and other deps in ``requirements.txt``).

Modules implement multi-seed comparisons against heterogeneous baselines, ablation
variants, and export figures into ``results/paper_figures/``. They are not imported by
``src/spinflow``; run scripts from the repository root.
"""
